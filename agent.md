# TrickTrace 开发记录

## 2026-09-08 · 整体合并发布

- 在最新 OCR 发布提交上合并单明手首攻、模拟次数选择、牌局删除和基本出牌知识展示；素材保存在源码目录，不打包为公开测试图片。
- 修复创建多个牌局后手机侧边栏撑宽页面的问题。
- 本次验证：38 项相关单元测试通过，生产构建通过；首攻专项完成 16 个分布与 13 张首攻排名；定约/表缓存/出牌位置回归、自动播放/暂停/手机布局回归均通过。
- 发布代码为 `b65b7dc`，已推送并核对 GitHub master；Cloudflare Pages 部署地址为 https://879895d6.tricktrace.pages.dev，生产地址 https://tricktrace.pages.dev/。
- 生产域名首攻专项通过：默认关闭、32/64/128/256 次选项、自定义 16 次模拟、13 张候选牌结果与手机布局正常。生产 OCR 上传回归通过：完整牌和防守牌共 77 张剩余牌正确，定约及当前墩正确；页面错误、跨站请求和图片 POST 均为零。

每轮改动都更新本文件，并与代码一起提交。记录实际完成的内容、验证结果和未完成事项；不要把计划或未通过的检查写成已完成。

## 2026-09-08 · 截图识牌与整图回归

- 分别使用文字/扇形稳定模型与低清平铺牌角模型，增加旋转右侧明手、重复牌角消除、覆盖牌角 6/9 方向处理、明手方位映射、当前墩检测、发牌方与局况识别。模糊定约明确标为候选。
- 修复分离的黑桃底座造成整门漏牌、文字行裁切过窄，以及 Q/J 字框重叠误删 J；低置信度牌点增加本地 OCR 复核。
- 7 张用户原图精确通过，311 张剩余牌正确；两张部分牌同时核对定约、庄家、发牌方、局况、引牌方和当前墩。标签和断言已加入 `npm run test:ocr`。
- 开发回归集从 25/80 整图全对提高到 68/80，正确牌张 4143/4160，候选共 4145；新增未用于调试的 20 图保留集为 19/20，1039/1040 正确，无额外牌。完整失败结果随仓库保存。
- 14 个缩放压缩变体的中间版本仅 8/14 全对，低清/扇形仍待改善；不保证任意截图全对。80 图已用于调试，不再作为未见过的保留集。
- 生产构建与 Chrome 上传流程通过，完整牌/防守牌 77 张剩余牌及当前墩正确进入编辑器；无页面错误、图片 POST 或跨站请求。工作区 41 项测试通过，含 250 个独立残局、2000 格参考表和 520 次原生/WASM 出牌对照。增加 Vitest 配置，排除本地 Emscripten 工具链自身的测试。
- 本条只提交识牌与验证相关文件；工作区已有单明手首攻、牌局管理和样式改动保留，未混入本条识牌发布。公开发布将在隔离检出构建、验证后记录。

### 本轮公开发布验证

- 代码提交 `1df5d5c` 已推送私有仓库并核对远端；从该提交的隔离检出构建，发布为 `https://451733eb.tricktrace.pages.dev`，生产地址为 `https://tricktrace.pages.dev/`。
- 线上通过真实文件上传入口验证 `双明手4.jpg` 和 `防守分析1.jpg`：77 张剩余牌正确，防守图的 3NT 西、发牌 S、EW 有局、北 D5 后东 DT 均正确。无页面错误、跨站资源请求或图片 POST；私有验证图未公开。报告见 `docs/validation/production-ocr-20260908.json`。

## 2026-09-07 · 单明手最佳首攻

- 侧边栏增加默认关闭的“单明手最佳首攻”开关；开启后才显示首攻分析页签。页签内始终显示 32/64/128/256 次预设和 16–2000 次自定义模拟数，不再受牌局就绪状态隐藏。
- 固定首攻方 13 张手牌，对其余 39 张做可复现随机采样，支持大牌点和花色长度约束。每个有效分布使用 DDS 比较所有合法首攻，输出击败定约率、95% 区间和庄家平均墩数。
- 完整牌局可一键生成只保留首攻手的新副本，不改动原牌局。
- 算法参考 Redeal `OpeningLeadSim` 的预发牌 + 约束过滤流程，以及 Killer Bridge Leads 的击败率、平均墩数和区间展示方式。本项目未接入叫牌引擎，因此约束由用户根据叫牌录入，不声称“叫牌重放验证”。
- 验证：38 项 Vitest 测试和生产构建通过；Chrome 实际验证功能默认关闭、开关/页签联动、新建副本不改原牌局，并完成 128 个有效分布的全首攻排序；重载后开关恢复默认关闭。

## 2026-09-06 · 手动录牌与按墩播放

- 增加四方选择器与四花色选牌面板，可以连续给同一家添加或移除牌；其他家已持有的牌禁用，并显示归属。保留文本输入。
- 增加 1–99 副号输入，联动四副循环的发牌方与十六副循环的局况，允许随后手动覆盖；副号随 PBN 导入导出保存。
- 牌桌保留刚完成一墩的四张牌与赢家，直到下一墩开始。
- 播放栏增加“自动”开关；自动模式墩内快速出牌，完成一墩停留至少 1 秒再继续。暂停、回退、重置与切换分支停止自动播放。
- 验证：23 项核心规则、格式与副号测试通过；生产构建通过。Chrome 实际流程验证连续选牌、移除、重复牌禁用、移动端无横向溢出、逐张开始、完整一墩保留、自动播放到第 52 张后停止、暂停有效。实测两次墩间停留为 1.028 秒和 1.008 秒。测试脚本：`scripts/validate_interactions.mjs`。
- 项目仍有识牌模型、复杂战术覆盖和公开部署等未完成工作；本条不代表整份原始计划已验收。

### 提交检查

- 清理交互验证脚本末尾的多余空行；不改变运行逻辑。重新检查暂存差异格式。

## 2026-09-06 · 定约交互、最小最优牌与识牌验证

- 点击定约标记进入编辑；定约表自动后台计算并复用，同牌局修改定约与逐张播放不重复发起表计算。点击至少 7 墩单元格设置对应阶数、花色、庄家。
- 桌中出牌按实际方位排成不重叠的十字，并随牌桌旋转；首攻选择器同步实际第一张牌。移动端保持定约按钮文字可见。
- 修复同一张牌出现在两家时无法从当前家移除的问题；已满 13 张仍能移除本家的重复牌。下方 NESW 输入框可编辑，与选牌按钮实时同步。
- 自动出牌与完整路线统一调用最优牌选择函数：只在 DDS 并列最优中选择最低点数，同点数按 S/H/D/C 固定顺序；不为了当前墩能赢而额外优先大牌。保留求解器最优值，不使用训练模型决定出牌。
- Worker 在空闲时复用，避免每出一张牌都重建 DDS；有正在执行的旧请求时仍可终止，过期结果不得覆盖当前局面。定约表使用独立 Worker。
- 字体换为 Noto Sans SC，400/600 两种字重，240 个 WOFF2 Unicode 分片，同站托管。HarmonyOS 分片包未采用：npm 包的 MIT 声明不能覆盖原字体对修改的限制。
- 识牌加入本地 Tesseract 复核、旋转牌角与十点数处理、原图候选框核对；修复文字牌谱相邻 K/J 字框重叠造成漏 J。新增两张用户截图和真实标签。完整 5 图回归为 4/5 完全正确，256/260 牌张正确，识别候选 259 张；第五张及部分手牌图仍需手工校对。
- 新训练候选使用 44,000 字符训练、4,000 字符验证，字符准确率 92.02%，但整图退步，已拒绝发布。保留较稳定的 20 类模型；合成训练不使用用户截图。
- 投入增加逐一续攻验证；简单挤牌增加长度看守、威胁张兑现和共同看守检查。双挤/连续挤牌仍为条件结构，复杂变种未全部完成，不宣称全覆盖。
- 复用 Python 3.11.6 `.venv`，其继承现有系统 GPU 库；将兼容的 ml-dtypes 安装到项目环境后 `pip check` 通过。新增版本依赖清单和隔离环境创建说明。
- 验证：34 项规则/副号/Worker/选牌/战术测试通过；250 个独立穷举残局、10 条完整最优牌路共 520 个原生/WASM 逐张对照重新通过。本轮较早还通过 100 个上游牌例的 2,000 格表对照。
- Chrome 本地交互复测通过：定约表仅 1 次请求、定约阶数、首攻同步、四方不重叠、重复 H3 修正、文本同步、移动视口无横向溢出及定约文字可见。自动播放两次实测墩间停留约 1.028 秒和 1.010 秒，并验证暂停及最后一墩保留。
- Cloudflare Pages 首个测试版已公开于 `https://tricktrace.pages.dev/`；本条新增改动将在构建与部署验证后发布。完整原始计划仍未全部验收，边界记录在 `docs/validation/status.md`。

### 发布验证与独立集结果

- 增加彩色文字牌谱的花色颜色/形状联合检查，减少黑桃被识为红桃导致的整门归属错误；黑白图仍采用形状分类。删除识牌临时 debugRows 输出。
- 完成 80 张独立合成牌谱：25 张整图完全正确，3881/4160 张正确，识别候选共 3883 张。召回由 84.30% 增至 93.29%，但整图全对由 29 降至 25，保留完整结果和失败记录，不把漏牌问题说成解决。
- 整理字体源文件下载路径，原始 TTF 仅放 `.tools`，公开目录只保留 WOFF2 分片；许可证文本仅清理尾随空白，未改正文条款。
- 提交 `ed2975e` 已推送并核对远端。构建通过，Cloudflare 部署完成：`https://7e19d025.tricktrace.pages.dev`，生产别名 `https://tricktrace.pages.dev/`。
- 线上 Chrome 回归通过：定约表只计算一次、点击表设置阶数、首攻同步、四方牌张不重叠、重复 H3 可移除、文本同步、移动定约文字可见。线上上传双明手4图识别 52 张正确，没有跨站资源请求、图片 POST 或页面错误；私有验证图片没有部署。
- 新增 Python 环境文档、依赖锁定、功能检查表、整图差异报告和生产验证脚本。复杂战术及低清/部分手牌识别仍待继续完善。
- 生产自动播放回归通过：墩内约 120 ms/张，两次墩间停留为 1.009 秒和 1.006 秒；暂停有效，52 张结束后停止并保留最后四张。报告随文档提交，未修改生产代码。
