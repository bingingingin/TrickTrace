# 新睿 CCBA 叫牌约束联动

## 依据

用户指定官网现行“新睿二盖一（CCBA）”，明确排除“简易二盖一”。2026-09-08 从[新睿官网](https://d.xinruibridge.com/)体系说明链接下载 [ccba_word_v2.2.zip](https://oss.xinruibridge.com/doc/ccba_word_v2.2.zip)。文档标识为《CCBA 体系说明》版本 2.2，北京新睿桥科技有限公司，2019.10。ZIP 的 SHA-256 为 `CB2684E275E7BB9CE9CE9395C536A7FBF25F84EC03B6C240C9E9AE448712E1FC`。

没有使用另列的 `nbs_word_v1.0.4.zip`（简易二盖一），也没有沿用官网较旧 HTML 的 CCBA 2.1。实现为自行编写的规则映射；原文档不随站点发布。

## 交互与数据

点选、粘贴、编辑、撤销和清空都会从当前完整叫牌串重新推断，不累积旧约束。支持花色符号、中文“不叫”、PASS 和 1N 简写。非法叫牌显示错误并禁用分析，不沿用之前合法串的推断。

未手动修改的字段跟随体系更新；手动修改的点力、单门长度、表达式分别覆盖对应自动字段。修改一门长度不会冻结另外三门。每家显示来源，可恢复本家或全部自动推断，也可显式设为不限。暂不反向根据手动点力猜测/改写叫牌，也不根据输入叫牌改动原牌局的定约。

“叫牌解释与规则来源”列出每个非 PASS 叫品的方位、序号、上下文含义与体系章节；未覆盖项单独提示。最终有效约束沿用既有采样器，不修改原牌例或首攻以外的实际手牌数据。

## 主要覆盖

| 规则组 | 体系章节 | 已实现内容 |
| --- | --- | --- |
| 开叫 | 1、2、3、4、5、6 | 一低花、一高花、第三家轻开、1NT、2NT、强二、弱二及第四家弱二差别 |
| 一低花应叫与再叫 | 1.1–1.3、1.7 | 一盖一、无将应叫、反加叫、弱跳应叫、部分 Walsh、低限/跳再叫、支持高花、v2.2 跳进高花局、双路重询 |
| 一高花应叫与再叫 | 2.1–2.7 | 半逼叫 1NT、二盖一逼局、弱跳、简单支持、伯根、杰可贝、Splinter、部分再叫、已不叫方逆朱瑞 |
| 1NT 后续 | 3.1–3.4 | 斯台曼及答叫、高花转移/超转移、德克萨斯、Smolen、部分转移后自然叫及示量叫、低花迫转 |
| 强二后续 | 4.1–4.2 | 等待应叫不硬设弱牌上限、积极应叫、无将再叫 22–24 / 25–27 / 28–30 |
| 2NT 后续 | 6.1–6.3 | 高花转移、傀儡斯台曼及部分后续、低花斯台曼、示量叫 |
| 争叫 | 3.9、防守叫牌 7、附录 | 自然直接争叫、直接/平衡位置 1NT、迈克尔斯、对强无将兰迪及自然争叫 |

代码入口是 `src/engine/ccba.ts`。只匹配明确上下文；没有将未知叫品自动当成自然花色的兜底。

## 近似和未覆盖部分

这不是完整 CCBA 叫牌机器人，也不是对生成牌逐副重放叫牌验证。多分支含义取安全的共同范围，未实现完整的“点力与牌型联合条件”分支。例如：

- 1NT 允许五张高花、六张低花和部分 5422，不再强制通用 `balanced`；当前只加各花色上界，可能接受体系不允许的其他形状。
- 2NT 的 19 点五张套例外暂以 19–21 的外包点力范围保留，没有强制 19 点必须五张套。
- 弱二长套允许降点；第三家轻开不硬定点力下限。强二只取各强牌分支的共同 HCP 下限，赢墩与速胜墩条件未实现。
- 含“好/差”或不同点力/配合分支的范围用外包区间；伯根 3D 的 9–12 包含 §2.6 的无局分支。调整点不转换成大牌点。
- 自然争叫按通常五张建模，特殊四张争叫可手动放宽；竞争叫牌后的普通应叫、支持性/技术性加倍等未全面覆盖。
- 不根据 PASS 排除所有可开叫/可应叫的牌，不自动处理所有满贯问叫、扣叫、勒本索尔、全部双路重询后续和高阶阻击。

推断出的自动字段可手动改写；覆盖不全会在页面提示，不宣称完整体系一致。

## 验证

61 项相关测试通过，其中 CCBA 专项 12 项，覆盖版本关键差异、座位映射、转移和斯台曼、已不叫方朱瑞、伯根/杰可贝/爆裂叫、再叫取交集、强二档位、傀儡斯台曼、撤销/清空、竞争叫牌隔离及逐字段覆盖。

生产构建上的 Chrome 回归通过：无需模板按钮即联动；改变转移目标更新正确花色；手动花色在改叫后保留；恢复后重新跟随；撤销去除旧推断；非法叫牌禁用分析；实际模拟、手机布局、原牌不变、原进度不变、删除/清空及刷新保持均通过。
