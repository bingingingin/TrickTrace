# UI 审查与主题更新（2026-09-08）

结论：建议局部重构，不建议整体推倒重做。现有侧栏牌局、中央四家牌桌、右侧分析的结构符合桥牌分析流程；深绿牌桌也有明确的产品辨识度。主要问题集中于响应式布局、文字层级和样式维护。

## 本次实现

- 页眉、页脚、浏览器图标和 Apple touch icon 使用 `logo/墩迹纯logo.png`；分享图片使用 `logo/墩迹宣传图.png`。运行时副本放在 `public/brand/`，保留原素材。
- 顶部明暗开关保存到 `tricktrace.theme`，与牌局数据独立。首次按系统主题初始化，选择后刷新保持；首屏脚本提前应用主题，减少闪白。
- `src/theme.css` 统一页面、表面、输入、选中和文字颜色，覆盖手牌、结果、编辑器与提示状态；浏览器主题色同步更新。
- 深色模式页眉和页脚使用新生成的 `logo/墩迹纯logo-白图黑底.png`，运行时资源为 `public/brand/tricktrace-mark-dark.png`；浅色模式继续使用原彩色 Logo。两版随主题开关切换。

## 美观度与重构优先级

| 观察 | 影响 | 建议 |
| --- | --- | --- |
| 桌面牌桌最小高度约 700px，中央留白较大 | 1000px 高窗口中回放操作接近底部，完整结果需要滚动 | 中优先级：压缩纵向留白，按视口高度调整桌面牌桌 |
| 390px 手机中，东西家手牌与中央出牌共占一行 | 长花色与数字角标空间紧张，点击目标偏小 | 高优先级：单独设计移动端牌桌布局，验证长花色与四张已出牌状态 |
| 辅助文字多处仅 9–12px | 手机上阅读费力，浅色弱文字对比不足 | 高优先级：统一字号层级，关键操作至少 12–14px，次要说明减少而非继续缩小 |
| 原 CSS 包含多轮同选择器与媒体查询覆盖 | 改一个断点容易影响其他布局 | 中优先级：按品牌、牌桌、结果、编辑器拆分样式，逐步将硬编码色迁入主题变量 |
| App.tsx 同时承担牌局状态、求解协调和大量页面 JSX | 视觉迭代涉及文件范围过大 | 中优先级：提取 Header、BoardSidebar、BridgeTable、AnalysisPanel，先保持交互契约 |
| 新 Logo 为深蓝与金色，界面以绿色为主 | 品牌与产品配色关联较弱，但并不冲突 | 低优先级：后续用深蓝品牌区与少量金色强调建立联系，牌桌保留绿色 |

不建议在本次 Logo/主题更新中同时改动全部信息架构。后续以“手机能清楚看牌和操作”为第一轮验收目标，再调整桌面密度与组件结构。

## 验证范围

- `npm run build`：TypeScript 和生产构建通过。
- Playwright：1440×1000、390×844 明暗主题截图；深色编辑器截图；深色刷新保持；390px 整页宽度等于视口宽度；控制台无 error/warning。
- 截图保存在 `output/playwright/`。检查覆盖默认示例牌局，不能据此声称所有长花色和 OCR 图片布局均已验证。
