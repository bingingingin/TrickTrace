# 验证状态（2026-09-08）

## 求解与交互

- DDS 与独立穷举比较 250 个随机小残局，包括未完成墩；通过。
- 100 个上游牌例、20 格表共 2,000 个结果与上游参考比较；通过。
- 10 条完整最优牌路逐张比较原生 DDS 与 WASM，共 520 次；每步合法，最终墩数一致。最低点数并列选择规则修改后重新通过。
- 34 项核心、牌号、Worker 复用、最小牌选择和战术测试通过。
- 浏览器回归检查定约修改、自动表计算次数、首攻同步、四方出牌不重叠、重复 H3 移除、文本/点牌同步、移动视口布局及自动播放。

原生 DDS 对照和浏览器 WASM 来自同一上游，不能替代独立算法验证，因此另外保留穷举残局测试。手机尺寸测试使用 Chrome 仿真视口，尚未完成真实手机性能验收。

## 战术

自建残局正反例见 `tests/tactics.test.ts`。投入检查防守方每一种合法续攻，间张兑现通过 DDS 后续路线检查；简单挤牌检查看守长度、全部垫牌选择、威胁张实际兑现及另一防家共同看守。搜索存在 20 秒预算与残局张数限制。

这些验证只覆盖实现的结构，不代表穷尽所有最优策略。双挤、连续挤牌目前仅为条件性组合结构；三门、将吃挤牌、进手相关挤牌及复杂剥光投入尚无完整分类验收。界面不得把条件事件当作强制战术证明。

## 识牌

2026-09-08 最新识别验证见 [ocr-release-20260908.md](ocr-release-20260908.md)：开发文字牌谱 80/80、既有留出集 20/20、原图 7/7 全对；压缩变体漏牌从 19 张减至 7 张，计入状态仍为 7/14 全对。最终生产构建本地上传验收覆盖 11 图、519 张牌。前一阶段 J 修复与测速见 [ocr-j-speed.md](ocr-j-speed.md)，以下保留历史记录；线上状态以 `agent.md` 最新发布条目为准。

用户提供的 5 张完整牌截图与 2 张部分牌截图不参加模型训练。标签位于 `tests/fixtures/recognition.json`，当前 7/7 原图精确通过：311 张剩余手牌正确，另核对部分牌的定约、庄家、发牌方、局况、当前墩和引牌方。坐庄图为 4NT 北、东家 SJ；防守图为 3NT 西、北 D5 后东 DT。定约包含基于不完整文字的候选，界面要求核对，测试匹配不代表置信度已经校准。

独立验证集来自上游牌例重新排版的 80 张合成牌谱，使用未参与训练的四种字体与不同分辨率。它衡量跨字体的文字牌谱效果，不能代表真实照片或其他应用所有布局。初始整图基线仅 29/80 张完全正确，牌张召回 3507/4160；原始报告保存为 `ocr-independent-baseline.json`，后续结果单独保存，不能覆盖基线。

本轮使用这 80 张图定位分离的黑桃底座、Q/J 字框重叠及低置信度牌点；因此它们现在属于开发回归集，不能继续视作未接触的保留集。完整结果为：

| 版本 | 整图完全正确 | 正确牌张 / 应有牌张 | 正确牌张 / 识别候选 |
| --- | --- | --- | --- |
| 早期基线 | 29/80 | 3507/4160（84.30%） | 3507/3746（93.62%） |
| 发布版 | 25/80 | 3881/4160（93.29%） | 3881/3883（99.95%） |
| 本轮候选 | 68/80 | 4143/4160（99.59%） | 4143/4145（99.95%） |

本轮仍有 17 张漏牌、2 张误认。上一发布版报告仍保存在 `ocr-independent-latest.json`，本轮结果在 `ocr-regression-20260908.json`，几何修复中间结果在 `ocr-independent-geometry.json`，均保留失败差异。

新增此前未用于本轮调试的 DDS 第 81–100 副重新排版图作为保留测试集：19/20 整图全对，1039/1040 张牌正确，没有额外牌；见 `ocr-holdout.json`。它仍是合成文字牌谱，不代表其他应用布局或真实照片的准确率。生成方式：`python scripts/generate_ocr_validation.py --start 80 --count 20 --output artifacts/ocr-holdout`，设置 `OCR_DIRECTORY=artifacts/ocr-holdout` 后运行独立验证脚本。

对用户 7 张图分别缩至 960/720 像素宽并以 JPEG 0.85 重新压缩，共 14 个变体；中间版本 8/14 精确通过，见 `ocr-variants.json`。低清实体牌和扇形牌仍会漏牌、混淆 A/4，不能保证任意截图完整识别。原图回归、开发回归、保留集和缩放变体分别报告，未将重复来源合并为独立样本数。

部署后在 `https://tricktrace.pages.dev/` 完成定约交互、表缓存、首攻、重复牌修正及移动视口回归，页面错误为零。通过真实文件上传入口验证“ 双明手4.jpg ”52 张全对；浏览器网络记录未出现图片 POST 或跨站资源请求，原始验证图不在公开部署文件中。生产部署资源对应代码提交 `ed2975e`。

此前 48,000 字符训练候选模型整图回归退步，未采用为通用模型。本轮将 12 度旋转增强模型仅用于低清平铺实体牌角；文字和扇形牌仍使用原模型。新增旋转明手解析与当前墩检测；缺牌不按 52 张自动补全。生产构建通过，真实文件上传回归检查完整牌与防守牌共 77 张剩余牌、定约和当前墩，未发生图片 POST、跨站资源请求或页面错误。新的公开发布状态以 `agent.md` 最新发布条目为准。
